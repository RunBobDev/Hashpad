# Build output.
dist/

# `graphify update .` belongs at the repo root, but running it from here
# creates a second copy. Ignored so a stray one cannot make `npm run lint`
# fail on generated JSON that is not ours to format.
graphify-out/

# Generated by `wails generate module` from the Go bindings. Formatting these
# would be undone the next time a bound method changes, and would put spurious
# churn in the diff of whichever task regenerated them.
wailsjs/
